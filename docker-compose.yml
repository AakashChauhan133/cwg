services:
  website:
    build: .
    container_name: carawingroup
    ports:
      - "8082:80"
    restart: unless-stopped
    networks:
      - default
      - proxy

networks:
  proxy:
    external: true
    name: mqtt-nginx_default


